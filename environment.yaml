# conda env create -f environment.yaml
name: mfwater
channels:
  - defaults
  - conda-forge
dependencies:
  - black
  - coverage
  - numpy
  - h5py
  - pre-commit
  - pylint
  - pytest
  - tox
  - pip:
      - covdefaults
